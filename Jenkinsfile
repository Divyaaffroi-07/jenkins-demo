pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Webhook Test'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing project'
            }
        }
    }
}
